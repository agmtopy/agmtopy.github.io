---
title: 罗马数字转整数_LeetCode07
date: 2020-06-03 23:43:20
categories: 算法
tags:
  - LeetCode
---

# 罗马数字转整数

## 题目

```
罗马数字包含以下七种字符: I， V， X， L，C，D 和 M。
字符          数值
I             1
V             5
X             10
L             50
C             100
D             500
M             1000

例如， 罗马数字 2 写做 II ，即为两个并列的 1。12 写做 XII ，即为 X + II 。 27 写做  XXVII, 即为 XX + V + II 。

通常情况下，罗马数字中小的数字在大的数字的右边。但也存在特例，例如 4 不写做 IIII，而是 IV。数字 1 在数字 5 的左边，所表示的数等于大数 5 减小数 1 得到的数值 4 。同样地，数字 9 表示为 IX。这个特殊的规则只适用于以下六种情况：

I 可以放在 V (5) 和 X (10) 的左边，来表示 4 和 9。
X 可以放在 L (50) 和 C (100) 的左边，来表示 40 和 90。 
C 可以放在 D (500) 和 M (1000) 的左边，来表示 400 和 900。
给定一个罗马数字，将其转换成整数。输入确保在 1 到 3999 的范围内。

```

- 示例 1:
```
输入: "III"
输出: 3
```

- 示例 2:
```
输入: "IV"
输出: 4
```
- 示例 3:
```
输入: "IX"
输出: 9
```

- 示例 4:
```
输入: "LVIII"
输出: 58
解释: L = 50, V= 5, III = 3.
```

- 示例 5:
```
输入: "MCMXCIV"
输出: 1994
解释: M = 1000, CM = 900, XC = 90, IV = 4
```

## 解题思路

> 这道题目类似于用key去查询对应的value的过程，按照既定的规则去判断

## 代码实现

```java
public class Solution_07 {

    private static Map<String,Integer> tempMap = new HashMap<>();
    private static Map<String,Integer> tempMap1 = new HashMap<>();

    static {
        tempMap.put("I",1);
        tempMap.put("V",5);
        tempMap.put("X",10);
        tempMap.put("L",50);
        tempMap.put("C",100);
        tempMap.put("D",500);
        tempMap.put("M",1000);

        tempMap1.put("IV",4);
        tempMap1.put("IX",9);
        tempMap1.put("XL",40);
        tempMap1.put("XC",90);
        tempMap1.put("CD",400);
        tempMap1.put("CM",900);
    }

    public static void main(String[] args) {
        System.out.println(doSolution("III"));
        System.out.println(doSolution("IV"));
        System.out.println(doSolution("IX"));
        System.out.println(doSolution("LVIII"));
        System.out.println(doSolution("MCMXCIV"));
    }

    public int romanToInt(String s) {
        return Solution_07.doSolution(s);
    }

    public static Integer doSolution(String s){
        if(s.length() == 1){
            return tempMap.get(s);
        }
        if(s.length() == 2 && tempMap1.containsKey(s)){
            return tempMap1.get(s);
        }
        String tempStr = s.substring(0,2);
        if(tempMap1.containsKey(tempStr)){
            return tempMap1.get(tempStr) + doSolution(s.substring(2,s.length()));
        }else{
            return tempMap.get(s.substring(0,1)) + doSolution(s.substring(1,s.length()));
        }
    }
}

```

执行结果：

![tDFSu4.png](https://s1.ax1x.com/2020/06/04/tDFSu4.png)

## 官方解题

在官方解题思路中还看到一种解法，很好。因此记录下来

![tDFdVs.png](https://s1.ax1x.com/2020/06/04/tDFdVs.png)

- 代码实现

```java
public class Solution_07_01 {

    public static void main(String[] args) {
        System.out.println(romanToInt("III"));
        System.out.println(romanToInt("IV"));
        System.out.println(romanToInt("IX"));
        System.out.println(romanToInt("LVIII"));
        System.out.println(romanToInt("MCMXCIV"));
    }

    private static Integer romanToInt(String s) {
        if (s.length() > 1){
            int tempInt = 0;
            if(transform(s.charAt(0)) >= transform(s.charAt(1))){
                tempInt = transform(s.charAt(0));
            }else{
                tempInt = -1*transform(s.charAt(0));
            }
            return tempInt + romanToInt(s.substring(1,s.length()));
        }
        return transform(s.charAt(0));
    }

    public static Integer transform(char c){
        switch (c){
            case 'I' : return 1;
            case 'V' : return 5;
            case 'X' : return 10;
            case 'L' : return 50;
            case 'C' : return 100;
            case 'D' : return 500;
            default: return 1000;
        }
    }
}

```

执行耗时虽然有所提高但是内存占用依旧很高，看来递归的方法处理问题会消耗大量的内存。